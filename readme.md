<img src="assets/logo.png" alt="PACE logo" width="56" align="left">

<h1>PACE</h1>

<strong>Prompt Accessibility Controlled Evaluation</strong>

<br clear="left">
<br>

[Yumeng Ma](https://www.yumengma.com/), [Jacob O. Wobbrock](https://faculty.washington.edu/wobbrock/), [Joe Devon](https://accessibility.day/about/), [Aaron Gustafson](https://www.aaron-gustafson.com/), [Michael Fairchild](https://github.com/mfairchild365), [Jennifer Mankoff](https://make4all.org/portfolio/jennifer-mankoff/), and [Venkatesh Potluri](https://venkateshpotluri.me/)

[![Repository Status](https://img.shields.io/badge/status-active-green)](#)
[![Artifact Type](https://img.shields.io/badge/artifact-benchmarking%20tool-blue)](#)

## Overview

PACE is a benchmarking tool for evaluating accessibility in HTML generated by large language models.

It supports controlled comparisons across:

- **models** — the LLMs being tested
- **system instructions** — setup messages given before each task
- **request variants** — different phrasings for the same component request
- **form component types** — HTML form elements such as text inputs, radio groups, and buttons

Each generated output is evaluated with two complementary layers:

1. **axe-core testing** in `bench/axe_runner.py`
2. **schema-based scoring** in `bench/scoring.py`

Together, these layers compare automated accessibility findings with component-specific structural checks.

## Repository Map

```text
pace/
├── app.py
├── bench/
│   ├── axe_runner.py
│   ├── io.py
│   ├── openrouter.py
│   ├── registry.py
│   ├── run_engine.py
│   └── scoring.py
├── data/
│   ├── components.json
│   ├── models.json
│   ├── prompt_conditions.json
│   ├── score.json
│   └── variants.json
├── scripts/
│   └── fetch_openrouter_models.py
├── assets/
├── output/
│   └── _runs/
└── README.md
```

## Quick Start

Clone the repository:

```bash
git clone https://github.com/momentine/pace.git
cd pace
```

Create and activate a virtual environment:

```bash
python -m venv .venv
source .venv/bin/activate
```

For Windows PowerShell:

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
```

Install dependencies:

```bash
pip install streamlit pandas numpy altair requests python-dotenv beautifulsoup4 lxml playwright
playwright install
npm install axe-core
```

Create a `.env` file in the project root:

```env
OPENROUTER_API_KEY=your_api_key_here
```

Refresh the OpenRouter model list:

```bash
python scripts/fetch_openrouter_models.py
```

Run the app:

```bash
streamlit run app.py
```

Streamlit usually starts at:

```text
http://localhost:8501
```

## A Tiny Tour

<p align="center">
  <img src="assets/settings.png" alt="PACE settings screen for editing models, system instructions, components, variants, axe, and schema settings" width="780">
</p>

<p align="center"><em>settings · tune the benchmark pieces</em></p>

<p align="center">
  <img src="assets/run.png" alt="PACE run screen for selecting benchmark conditions and launching a run" width="780">
</p>

<p align="center"><em>run · choose a benchmark set and launch it</em></p>

<p align="center">
  <img src="assets/results.png" alt="PACE results screen for reviewing benchmark outputs and comparing axe-core and schema-based results" width="780">
</p>

<p align="center"><em>results · read the outputs and compare conditions</em></p>

## Running a Benchmark

1. Open the Streamlit app.
2. Go to **Settings**.
3. Inspect or edit the registries.
4. Go to **Run**.
5. Select models, system instructions, request variants, component tasks, and repetitions.
6. Click **Run**.

Run time depends on the selected models, tasks, and repetitions.

## Results

Each benchmark run is saved under:

```text
output/_runs/
```

A run directory may contain:

```text
results.csv
per_check.csv
generated HTML files
```

`results.csv` stores trial-level results.

`per_check.csv` stores detailed accessibility check results.

## Citation

This work has not been published yet. The official ACM citation, DOI badge, and BibTeX entry will be added after publication.

## Accessibility Notes

PACE studies accessibility, so this repository should remain usable with assistive technologies.

Please keep headings descriptive, give images meaningful alt text, use language tags for code blocks, and report accessibility barriers through repository issues or direct contact with the maintainers.

## Project Scope

PACE currently evaluates AI-generated HTML form components. The tool can be adapted to other interface outputs by changing component definitions, scoring rules, and evaluation pipelines.