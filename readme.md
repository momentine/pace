# PACE  
**Prompt Accessibility Controlled Evaluation**

PACE is a benchmarking framework for evaluating the accessibility of HTML generated by large language models (LLMs).

It supports controlled experiments across models, prompt conditions, prompt variants, and component tasks. Each generation is evaluated with two complementary methods:

- **Schema-based scoring** in `bench/scoring.py`
- **axe-core testing** in `bench/axe_runner.py`

---

## Overview

PACE helps you answer questions like these:

- Which models produce more accessible HTML?
- How much does prompt wording change accessibility outcomes?
- Which prompt conditions improve semantic structure?
- Which accessibility checks fail most often across components?

---

## Repository Layout

```text
pace/
├── app.py
├── bench/
│   ├── axe_runner.py
│   ├── io.py
│   ├── openrouter.py
│   ├── registry.py
│   ├── run_engine.py
│   └── scoring.py
├── data/
│   ├── components.json
│   ├── prompt_conditions.json
│   ├── score.json
│   ├── selected_models.json
│   └── variants.json
├── assets/
│   └── logo.svg
├── output/
│   └── _runs/
└── README.md
```

---

## What Each Part Does

### `app.py`
Main Streamlit app.

Use it to:

- configure benchmark runs
- inspect and edit registries
- launch experiments
- view results

### `bench/run_engine.py`
Core execution engine.

It:

- loops through benchmark combinations
- sends prompts to models
- saves generated HTML
- runs schema scoring
- runs axe-core checks
- writes output files for analysis

### `bench/scoring.py`
Schema-based accessibility evaluator.

It parses generated HTML and scores checks such as:

- label association
- description association
- required state encoding
- input type correctness
- error structure
- group semantics
- status announcements

Each check returns:

- a score from **0 to 2**
- a short rationale

### `bench/axe_runner.py`
axe-core integration for automated accessibility testing.

It:

- wraps fragments in a minimal document
- loads `axe.min.js`
- runs configured rules
- captures violations, passes, and incomplete checks

### `bench/openrouter.py`
OpenRouter integration.

It handles:

- prompt submission
- model responses
- token usage
- cost estimates

### `bench/registry.py`
Registry loader and manager.

It works with:

- models
- prompt conditions
- variants
- components
- scoring definitions

---

## Configuration Files

### `data/components.json`
Defines benchmark tasks.

Examples:

- text inputs
- email fields
- radio groups
- file uploads
- buttons

### `data/variants.json`
Defines prompt wording variants.

Examples:

- action command
- short specification
- descriptive object phrase
- build instruction
- standards-oriented phrasing

### `data/prompt_conditions.json`
Defines higher-level prompting conditions.

Examples:

- baseline
- strict specification
- front-end engineering persona
- accessibility-focused perspective

### `data/score.json`
Defines the scoring rubric and check mappings.

The scoring logic itself lives in `bench/scoring.py`.

---

## How PACE Runs

For each trial, PACE does the following:

1. selects a model
2. selects a prompt condition
3. selects a prompt variant
4. selects a component task
5. generates HTML
6. saves the output
7. runs schema-based scoring
8. runs axe-core evaluation
9. records the results

This creates a fully crossed benchmark design.

---

## Quick Start

### 1. Clone the repository

```bash
git clone https://github.com/momentine/pace.git
cd pace
```

### 2. Create and activate a virtual environment

#### macOS / Linux

```bash
python -m venv .venv
source .venv/bin/activate
```

#### Windows PowerShell

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
```

#### Windows Command Prompt

```bat
python -m venv .venv
.venv\Scripts\activate.bat
```

### 3. Install Python dependencies

```bash
pip install streamlit pandas numpy altair requests python-dotenv beautifulsoup4 lxml playwright
```

### 4. Install Playwright browsers

```bash
playwright install
```

### 5. Install `axe-core`

```bash
npm install axe-core
```

PACE expects this file to be available:

```text
node_modules/axe-core/axe.min.js
```

---

## Environment Setup

Create a `.env` file in the project root:

```env
OPENROUTER_API_KEY=your_api_key_here
OPENROUTER_HTTP_REFERER=http://localhost:8501
OPENROUTER_X_TITLE=PACE
```

Minimum required value:

```env
OPENROUTER_API_KEY=your_api_key_here
```

---

## Run the App

```bash
streamlit run app.py
```

Streamlit will usually start on:

```text
http://localhost:8501
```

Open that address in your browser.

---

## Run a Benchmark

1. Open the app.
2. Go to the **Settings** panel.
3. Inspect or edit the registries.
4. Go to the **Run** tab.
5. Select:
   - models
   - prompt conditions
   - variants
   - components
   - repetition count
6. Click **Run Benchmark**.

---

## Output

Each run creates a directory under:

```text
output/_runs/
```

Example:

```text
output/_runs/run_123456/
```

A run directory may include:

```text
results.csv
per_check.csv
generated HTML files
axe JSON results
```

### `results.csv`
One row per generation trial.

Typical fields:

- model
- system instruction
- variant
- component
- repetition index
- token usage
- cost
- schema score
- axe violations

### `per_check.csv`
Detailed accessibility check results.

Typical fields:

- check ID
- score
- explanation
- associated output file

---

## Notes

- PACE currently focuses on HTML-only interface generation.
- Schema scoring and axe-core capture different accessibility signals.
- Generated runs are stored in `output/_runs/`.
- Those run artifacts are typically excluded from version control.

---

## One-Command Setup Example

After cloning the repo, a typical setup flow looks like this:

```bash
python -m venv .venv
source .venv/bin/activate
pip install streamlit pandas numpy altair requests python-dotenv beautifulsoup4 lxml playwright
playwright install
npm install axe-core
streamlit run app.py
```

---

## Summary

PACE is a controlled benchmarking framework for studying accessibility in LLM-generated HTML through systematic variation across:

- models
- prompt conditions
- prompt variants
- component tasks
